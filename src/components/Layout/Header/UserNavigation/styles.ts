import styled from 'styled-components';

export const Container = styled.div`
  width: 96px;

  ul {
    display: flex;
    justify-content: space-between;
    width: 100%;

    li {
      list-style: none;
    }
  }
`
